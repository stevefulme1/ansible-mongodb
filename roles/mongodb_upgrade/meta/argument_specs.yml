---
argument_specs:
  main:
    short_description: "Rolling upgrade of MongoDB cluster"
    options:
      mongodb_upgrade_host:
        description: Target host address
        type: str
        required: true
      mongodb_upgrade_username:
        description: Authentication username
        type: str
      mongodb_upgrade_password:
        description: Authentication password
        type: str
      mongodb_upgrade_api_key:
        description: API key for authentication
        type: str
      mongodb_upgrade_validate_certs:
        description: Validate SSL certificates
        type: bool
        default: true
      mongodb_upgrade_state:
        description: Desired state
        type: str
        default: present
        choices: [present, absent]
