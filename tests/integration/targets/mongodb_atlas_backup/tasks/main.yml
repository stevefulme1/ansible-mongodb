---
- name: Test mongodb_atlas_backup module
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Create atlas_backup (check mode)
      stevefulme1.mongodb.mongodb_atlas_backup:
        host: "{{ test_host }}"
        name: "test-atlas-backup"
        state: present
      check_mode: true
      register: check_result
      ignore_errors: true

    - name: Verify check mode reports changed
      ansible.builtin.assert:
        that:
          - check_result is changed or check_result is failed
        fail_msg: "Check mode did not report changed"

    - name: List atlas_backups
      stevefulme1.mongodb.mongodb_atlas_backup_info:
        host: "{{ test_host }}"
      register: list_result
      ignore_errors: true

    - name: Verify list returns a list
      ansible.builtin.assert:
        that:
          - list_result.atlas_backups is defined or list_result is failed
        fail_msg: "List did not return expected structure"
