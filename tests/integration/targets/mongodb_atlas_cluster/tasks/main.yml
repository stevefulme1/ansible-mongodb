---
- name: Test mongodb_atlas_cluster module
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Create atlas_cluster (check mode)
      stevefulme1.mongodb.mongodb_atlas_cluster:
        host: "{{ test_host }}"
        name: "test-atlas-cluster"
        state: present
      check_mode: true
      register: check_result
      ignore_errors: true

    - name: Verify check mode reports changed
      ansible.builtin.assert:
        that:
          - check_result is changed or check_result is failed
        fail_msg: "Check mode did not report changed"

    - name: List atlas_clusters
      stevefulme1.mongodb.mongodb_atlas_cluster_info:
        host: "{{ test_host }}"
      register: list_result
      ignore_errors: true

    - name: Verify list returns a list
      ansible.builtin.assert:
        that:
          - list_result.atlas_clusters is defined or list_result is failed
        fail_msg: "List did not return expected structure"
