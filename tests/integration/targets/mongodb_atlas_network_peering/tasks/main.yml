---
- name: Test mongodb_atlas_network_peering module
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Create atlas_peering (check mode)
      stevefulme1.mongodb.mongodb_atlas_network_peering:
        host: "{{ test_host }}"
        name: "test-atlas-peering"
        state: present
      check_mode: true
      register: check_result
      ignore_errors: true

    - name: Verify check mode reports changed
      ansible.builtin.assert:
        that:
          - check_result is changed or check_result is failed
        fail_msg: "Check mode did not report changed"

    - name: List atlas_peerings
      stevefulme1.mongodb.mongodb_atlas_network_peering_info:
        host: "{{ test_host }}"
      register: list_result
      ignore_errors: true

    - name: Verify list returns a list
      ansible.builtin.assert:
        that:
          - list_result.atlas_peerings is defined or list_result is failed
        fail_msg: "List did not return expected structure"
